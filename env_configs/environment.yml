name: flame_ai
channels:
  - defaults
  - pytorch
  - conda-forge
dependencies:
  - python=3.9
  - cudatoolkit=11.4
  - cudnn
  - pytorch
  - pip
  - pip:
      - pip-tools
      - setuptools==59.5.0
